<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | GoCotano</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
  <%- include('../../partials/header') %>

  <div class="container" style="max-width: 720px;">
    <div class="d-flex justify-between align-center" style="margin-bottom: 1rem;">
      <h1 style="color: var(--primary-blue);">Add Menu Item</h1>
      <a href="/restaurant/menu" class="btn btn-secondary">Back to Menu</a>
    </div>

    <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>
    <% if (typeof success !== 'undefined' && success && success.length > 0) { %>
      <div class="alert alert-success"><%= success %></div>
    <% } %>

    <div class="card">
      <div class="card-header">
        <h2 class="card-title">New Item Details</h2>
      </div>
      <div class="card-body">
        <form action="/restaurant/menu" method="POST" enctype="multipart/form-data">
          <div class="form-grid">
            <div class="form-group">
              <label for="name" class="form-label">Name</label>
              <input type="text" id="name" name="name" class="form-control" required placeholder="e.g., Chicken Adobo">
            </div>

            <div class="form-group">
              <label for="price" class="form-label">Price</label>
              <input type="number" id="price" name="price" class="form-control" required step="0.01" min="0.01" placeholder="e.g., 199.00">
            </div>

            <div class="form-group">
              <label for="category" class="form-label">Category</label>
              <input type="text" id="category" name="category" class="form-control" required placeholder="Main, Appetizer, Beverage, Dessert">
            </div>

            <div class="form-group">
              <label for="imageFile" class="form-label">Upload Image</label>
              <input type="file" id="imageFile" name="imageFile" class="form-control" accept="image/*">
              <small class="hint">JPG, PNG, WEBP, GIF up to 5MB</small>
            </div>
          </div>

          <div class="form-group">
            <label for="description" class="form-label">Description</label>
            <textarea id="description" name="description" class="form-control" rows="4" required placeholder="Short tasty description..."></textarea>
          </div>

          <div class="form-grid">
            <div class="form-group">
              <label for="nameFilipino" class="form-label">Name (Filipino)</label>
              <input type="text" id="nameFilipino" name="nameFilipino" class="form-control" placeholder="optional">
            </div>

            <div class="form-group">
              <label for="descFilipino" class="form-label">Description (Filipino)</label>
              <input type="text" id="descFilipino" name="descFilipino" class="form-control" placeholder="optional">
            </div>
          </div>

          <div class="form-grid">
            <div class="form-group">
              <label class="form-checkbox">
                <input type="checkbox" name="available" checked>
                <span>Available</span>
              </label>
            </div>
            <div class="form-group">
              <label class="form-checkbox">
                <input type="checkbox" name="isSpicy">
                <span>Spicy</span>
              </label>
            </div>
            <div class="form-group">
              <label class="form-checkbox">
                <input type="checkbox" name="isVegetarian">
                <span>Vegetarian</span>
              </label>
            </div>
          </div>

          <button type="submit" class="btn btn-primary">Add Item</button>
        </form>
      </div>
    </div>
  </div>

  <%- include('../../partials/footer') %>
</body>
</html>
          <div class="form-group">
            <label for="image" class="form-label">Or Image URL (optional)</label>
            <input type="url" id="image" name="image" class="form-control" placeholder="https://...">
            <small class="hint">If provided, uploaded file takes precedence.</small>
          </div>

