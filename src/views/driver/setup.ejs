<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | GoCotano</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <meta name="robots" content="noindex">
  <style>
    .hint {
      color: var(--gray);
      font-size: 0.875rem;
      margin-top: 0.25rem;
    }
    .welcome-section {
      background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-blue-dark) 100%);
      color: var(--white);
      padding: 2rem;
      border-radius: var(--radius-lg);
      margin-bottom: 2rem;
      text-align: center;
    }
    .welcome-section h1 {
      margin: 0 0 0.5rem 0;
      font-size: 2rem;
    }
    .welcome-section p {
      margin: 0;
      font-size: 1.1rem;
      opacity: 0.9;
    }
    .info-card {
      background: var(--primary-blue-lighter);
      border-left: 4px solid var(--primary-blue);
      padding: 1rem;
      border-radius: var(--radius-md);
      margin-bottom: 1.5rem;
    }
    .info-card h3 {
      margin: 0 0 0.5rem 0;
      color: var(--primary-blue-dark);
      font-size: 1.1rem;
    }
    .info-card p {
      margin: 0;
      color: var(--dark-gray);
      font-size: 0.95rem;
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <div class="container" style="max-width: 640px; margin-top: 2.5rem;">
    <div class="welcome-section">
      <h1>🚗 Welcome, Driver!</h1>
      <p>Complete your profile to start earning with GoCotano</p>
    </div>

    <div class="card">
      <div class="card-header">
        <h2 class="card-title text-center">Setup Your Driver Profile</h2>
      </div>
      <div class="card-body">
        <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
          <div class="alert alert-error"><%= error %></div>
        <% } %>

        <% if (typeof success !== 'undefined' && success && success.length > 0) { %>
          <div class="alert alert-success"><%= success %></div>
        <% } %>

        <div class="info-card">
          <h3>📝 Before You Start</h3>
          <p>Make sure you have your vehicle information and license details ready. You'll need these to complete your registration.</p>
        </div>

        <form action="/driver/setup" method="POST">
          <div class="form-group">
            <label for="vehicleType" class="form-label">Vehicle Type <span style="color: var(--primary-orange);">*</span></label>
            <select id="vehicleType" name="vehicleType" class="form-control" required>
              <option value="">Select vehicle type</option>
              <option value="Motorcycle">🏍️ Motorcycle</option>
              <option value="Car">🚗 Car</option>
              <option value="Bicycle">🚴 Bicycle</option>
              <option value="Van">🚐 Van</option>
            </select>
            <div class="hint">Choose the vehicle you'll use for deliveries</div>
          </div>

          <div class="form-group">
            <label for="vehicleNumber" class="form-label">Vehicle Plate Number <span style="color: var(--primary-orange);">*</span></label>
            <input
              type="text"
              id="vehicleNumber"
              name="vehicleNumber"
              class="form-control"
              required
              placeholder="e.g., ABC-1234"
              style="text-transform: uppercase;"
            >
            <div class="hint">Enter your vehicle's plate number</div>
          </div>

          <div class="form-group">
            <label for="licenseNumber" class="form-label">Driver's License Number</label>
            <input
              type="text"
              id="licenseNumber"
              name="licenseNumber"
              class="form-control"
              placeholder="e.g., N01-12-345678"
              style="text-transform: uppercase;"
            >
            <div class="hint">Optional - Your professional driver's license number</div>
          </div>

          <div class="form-group">
            <label for="currentCity" class="form-label">Operating City <span style="color: var(--primary-orange);">*</span></label>
            <select id="currentCity" name="currentCity" class="form-control" required>
              <option value="">Select your city</option>
              <option value="Manila">Manila</option>
              <option value="Quezon City">Quezon City</option>
              <option value="Makati">Makati</option>
              <option value="Pasig">Pasig</option>
              <option value="Taguig">Taguig</option>
              <option value="Pasay">Pasay</option>
              <option value="Mandaluyong">Mandaluyong</option>
              <option value="Caloocan">Caloocan</option>
              <option value="Cebu City">Cebu City</option>
              <option value="Davao City">Davao City</option>
              <option value="Cagayan de Oro">Cagayan de Oro</option>
              <option value="Other">Other</option>
            </select>
            <div class="hint">Select the city where you'll be making deliveries</div>
          </div>

          <div class="info-card" style="background: var(--primary-orange-lighter); border-left-color: var(--primary-orange);">
            <h3>💰 Earn While You Drive</h3>
            <p>Start earning immediately after setup. You'll receive delivery requests in real-time and can track your earnings in your dashboard.</p>
          </div>

          <button type="submit" class="btn btn-primary w-100" style="margin-top: 1rem; font-size: 1.1rem; padding: 0.875rem;">
            Create Driver Profile
          </button>
        </form>

        <p class="text-center" style="margin-top: 1.25rem;">
          <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
        </p>
      </div>
    </div>
  </div>

  <%- include('../partials/footer') %>

  <script>
    // Auto-uppercase input fields
    document.addEventListener('DOMContentLoaded', function() {
      const vehicleNumberInput = document.getElementById('vehicleNumber');
      const licenseNumberInput = document.getElementById('licenseNumber');

      vehicleNumberInput.addEventListener('input', function() {
        this.value = this.value.toUpperCase();
      });

      if (licenseNumberInput) {
        licenseNumberInput.addEventListener('input', function() {
          this.value = this.value.toUpperCase();
        });
      }
    });
  </script>
</body>
</html>
